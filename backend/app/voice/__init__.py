"""Voice (TTS) — Sarvam in production, the browser's built-in speech as the free dev path.

Like the LLM layer: build on the free path now (browser SpeechSynthesis, no key), swap to
Sarvam Bulbul (Indian voices, per-character) before launch by setting SARVAM_API_KEY.
STT (Sarvam Saarika / browser SpeechRecognition) is the next voice step — not yet wired.
"""
